# ══════════════════════════════════════════════════════════════
#  UniSync — Python Dependencies
#  Install with: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# ── Core Framework ─────────────────────────────────────────────
Flask==3.0.3
Werkzeug==3.0.3

# ── Environment ────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Database ───────────────────────────────────────────────────
supabase==2.5.3

# ── Email ──────────────────────────────────────────────────────
Flask-Mail==0.10.0

# ── Background Jobs (local only — disabled on Vercel) ──────────
Flask-APScheduler==1.13.1

# ── Excel Parsing (lazy import — only loaded by admin upload) ──
pandas==2.2.2
openpyxl==3.1.3

# ── WSGI Server (local production only — Vercel uses its own runner) ──
# gunicorn is not used on Vercel. Install locally if needed:
#   pip install gunicorn==22.0.0